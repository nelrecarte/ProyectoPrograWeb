# npm 10 truena con "Cannot read properties of null (reading 'edgesOut')" al
# resolver el arbol de vitest 4. Con esto el install funciona igual en npm 10
# y en npm 12, sin tener que pedirle a nadie que actualice npm.
legacy-peer-deps=true
